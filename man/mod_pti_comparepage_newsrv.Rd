% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_pti_comparepage.R
\name{mod_pti_comparepage_newsrv}
\alias{mod_pti_comparepage_newsrv}
\title{Comparison-page server module}
\usage{
mod_pti_comparepage_newsrv(
  id,
  shp_dta,
  map_dta,
  wt_dta,
  active_tab,
  target_tabs,
  mtdtpdf_path = ".",
  shapes_path = ".",
  show_adm_levels = NULL,
  ...
)
}
\arguments{
\item{id}{Character. Shiny module namespace ID. Must match the `id`
passed to [mod_pti_comparepage_ui()].}

\item{shp_dta}{Reactive yielding a named list of `sf` tibbles (one
per admin level). Forwarded to both [mod_plot_pti2_srv()] children.}

\item{map_dta}{Reactive yielding the structured PTI list (output of
[mod_ptipage_newsrv()]).}

\item{wt_dta}{Reactive yielding the weights list (output of the
weights-input module).}

\item{active_tab}{Reactive character indicating the currently selected
tab.}

\item{target_tabs}{Character vector of tab names this module should
render for.}

\item{mtdtpdf_path}{Character. Filesystem path to the metadata PDF
served via the side-panel download link in each map.}

\item{shapes_path}{Character. Filesystem path to the shapes archive
served via the side-panel download link in each map.}

\item{show_adm_levels}{Character vector or NULL. Admin levels to
expose in the side-panel selector of each map.}

\item{...}{Forwarded to both [mod_plot_pti2_srv()] children.}
}
\value{
No explicit return value. Called for side effects (renders
  two side-by-side leaflet maps within the Shiny session).
}
\description{
Server module powering the comparison page. Instantiates two
[mod_plot_pti2_srv()] sub-modules (`first_leaf` / `second_leaf`)
against the same shapes, structured PTI data, and weights. The two
maps share inputs but render independently so the user can pan / zoom
each side separately while comparing the same underlying weighting.
}
\examples{
\dontrun{
mod_pti_comparepage_newsrv(
  "compare",
  shp_dta     = shp_dta,
  map_dta     = map_dta,
  wt_dta      = wt_dta,
  active_tab  = active_tab,
  target_tabs = "Compare PTIs"
)
}
}
